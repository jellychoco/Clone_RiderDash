import React, { Fragment } from "react";

const Page = () => {
  return (
    <Fragment>
      <header>header</header>
      <main>main</main>
      <footer>footer</footer>
    </Fragment>
  );
};

export default Page;
